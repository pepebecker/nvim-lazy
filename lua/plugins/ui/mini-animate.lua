return {
  "echasnovski/mini.animate",
  opts = function(_, opts)
    opts.scroll = {
      enable = false,
    }
    opts.open = {
      enable = false,
    }
    opts.close = {
      enable = false,
    }
  end,
}
